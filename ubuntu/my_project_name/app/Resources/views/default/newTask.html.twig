{#It disables the browsers native validation to be able to test server side validation#}
{#{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}#}
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}